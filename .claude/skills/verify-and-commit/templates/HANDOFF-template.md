# HANDOFF — <project>

The implementation diary. Read it before starting; update it before finishing.
A task is not complete until this file reflects the work.

**Status**: <GREEN | YELLOW | RED> — <one line: where the work stands right now>
**Date**: <last updated, YYYY-MM-DD>
**Phase**: <spec-NN / loop phase the diary sits between, e.g. "awaiting
verify-and-commit for spec-02">

---

## Done this session

- **Implemented**: <what landed — spec, files, behaviours>
- **Outcome**: <gate result, test counts raw one line per binary (never summed),
  what was verified>
- **Committed as**: <commit hash, or "uncommitted — see Left uncommitted">

## Remains — resume here

1. <the FIRST thing the next session does — the entry point>
2. <...>

## Key files

- <authoritative specs/docs the next session must read first>

## Open issues

<!-- Known defects, wrong premises found in specs, anything deferred.
     A blocker recorded here is worth more than a blocker remembered. -->

## Last run results

<!-- What the last pi-workhorse run actually did: gate stages, failures,
     measured findings, bugs exposed — so the next session does not re-derive
     them. Raw numbers, not summaries. -->

## Left uncommitted

<!-- Anything the mandatory checkpoint commit did not cover. -->

## Do not repeat

<!-- Things already tried that did not work, so the next session does not
     re-derive them. -->
